{% extends 'base.html.twig' %}

{% block title %}StateHistory{% endblock %}

{% block body %}
    <h1>StateHistory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ state_history.id }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{{ state_history.datetime ? state_history.datetime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_state_history_index') }}">back to list</a>

    <a href="{{ path('app_state_history_edit', {'id': state_history.id}) }}">edit</a>

    {{ include('state_history/_delete_form.html.twig') }}
{% endblock %}
